import { ToneMapping, WebGLRenderer, WebGLRenderTarget } from 'three';
import { ShaderPass2 } from './ShaderPass2';
import { UiObjectConfig } from 'webgi/interfaces';
export declare const Uncharted2Tonemapping: ToneMapping;
export declare class TonemapPass extends ShaderPass2 {
    uiConfig?: UiObjectConfig;
    toneMapping: ToneMapping;
    tonemapBackground: boolean;
    clipBackground: boolean;
    exposure: number;
    constructor(useDepth?: boolean, screenShaderMain?: string, screenShaderPars?: string);
    render(renderer: WebGLRenderer, writeBuffer: WebGLRenderTarget, readBuffer: WebGLRenderTarget, deltaTime: number, maskActive: boolean): void;
}
//# sourceMappingURL=TonemapPass.d.ts.map